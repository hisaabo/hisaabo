---
/**
 * Content visible when persona is "ca" or "all" (no persona selected).
 * Usage: <ForCA>This shows for CAs and when no persona is active.</ForCA>
 */
---
<div class="persona-ca">
  <slot />
</div>
