[[permission]]
identifier = "allow-clear-session-token"
description = """
Allows the main window to call clear_session_token, which removes
the Hisaabo session token from the OS keyring.
Idempotent: succeeds even when no token is currently stored.
"""
commands.allow = ["clear_session_token"]
